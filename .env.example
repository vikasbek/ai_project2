# Used by docker-compose.yml to fill in the MongoDB Atlas connection string
# for every service. Copy this file to .env (gitignored) and set the real
# password for the vikasbek35_db_user Atlas user.
MONGO_PASSWORD=changeme
